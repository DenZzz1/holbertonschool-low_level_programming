#include <stdio.h>

/**
 * main - Affiche un message avec printf
 *
 * Description: Cette fonction affiche le message
 * "with proper grammar, but the outcome is a piece of art,"
 * suivi d’un retour à la ligne à l'aide de la fonction printf.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
